

module.exports.getArticles = async (authorId, status) => {
    let result = [];
    // get all articles with status for author
    // if status === '' return all
    // extract simple data
    return result;
}

module.exports.getCorresponcingAuthors = async (articleId) => {
    let result = [];
    // get all authors
    // rank them by correspondance
    return result;
}